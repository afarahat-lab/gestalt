{
  "name": "@openharness/cli",
  "version": "0.1.0",
  "private": true,
  "description": "Developer-facing CLI tool — harness init, run, status, logs, dashboard",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  }
}
