{
  "name": "@gestalt/core",
  "version": "0.1.0",
  "private": true,
  "description": "Core harness engine — config, logger, LLM, queue, repository, harness",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@langchain/langgraph-checkpoint-postgres": "^0.0.1",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "bullmq": "^5.7.0",
    "globby": "^14.0.0",
    "pino": "^9.0.0",
    "pino-pretty": "^11.0.0",
    "yaml": "^2.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
