{
  "name": "@agentforge-sdlc/core",
  "version": "0.1.0",
  "private": true,
  "description": "Core harness engine — context management, agent protocol, feedback signals, LLM abstraction, repository interface",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  }
}
