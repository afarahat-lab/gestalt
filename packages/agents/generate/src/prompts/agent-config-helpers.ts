/**
 * Agent config helpers — re-export shim.
 *
 * Implementation moved to `@gestalt/core/agents/agent-config-helpers`
 * in 2026-06. Existing import paths keep working.
 */
export {
  applyAgentConfig,
  buildPersona,
  buildExtensionsBlock,
} from '@gestalt/core';
