{
  "name": "@openharness/agents-generate",
  "version": "0.1.0",
  "private": true,
  "description": "Generate layer agents — design, context, code, test, lint-config",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  }
}
