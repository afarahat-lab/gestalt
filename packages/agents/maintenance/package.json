{
  "name": "@agentforge-sdlc/agents-maintenance",
  "version": "0.1.0",
  "private": true,
  "description": "Maintenance layer agents — drift, alignment, gc, evaluation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  }
}
