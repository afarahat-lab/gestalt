{
  "name": "@gestalt/agents-deploy",
  "version": "0.1.0",
  "private": true,
  "description": "Deploy layer agents — PR, pipeline, promotion",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@gestalt/core": "workspace:*",
    "@gestalt/agents-quality-gate": "workspace:*",
    "simple-git": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
