{
  "name": "@agentforge-sdlc/agents-quality-gate",
  "version": "0.1.0",
  "private": true,
  "description": "Quality gate agents — constraint, test-runner, lint, security, review",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  }
}
