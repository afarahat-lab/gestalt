{
  "name": "@openharness/dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "React oversight dashboard — intent history, agent activity, alerts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  }
}
