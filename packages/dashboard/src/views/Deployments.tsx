/**
 * Deployments view — Environment status, pending promotions with approve/reject actions.
 * Full implementation: Phase 2.
 */
import type React from 'react';

export function Deployments(): React.ReactElement {
  throw new Error('Deployments not yet implemented — pending Phase 2');
}
