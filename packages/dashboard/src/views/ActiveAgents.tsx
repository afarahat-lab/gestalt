/**
 * ActiveAgents view — Real-time view of currently running agent executions.
 * Full implementation: Phase 2.
 */
import type React from 'react';

export function ActiveAgents(): React.ReactElement {
  throw new Error('ActiveAgents not yet implemented — pending Phase 2');
}
