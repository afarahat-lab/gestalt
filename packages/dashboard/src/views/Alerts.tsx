/**
 * Alerts view — Primary human action surface — open alerts requiring a decision, sorted by severity.
 * Full implementation: Phase 2.
 */
import type React from 'react';

export function Alerts(): React.ReactElement {
  throw new Error('Alerts not yet implemented — pending Phase 2');
}
